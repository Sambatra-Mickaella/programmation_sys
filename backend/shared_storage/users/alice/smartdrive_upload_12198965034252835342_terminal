chsh -s $(which fish) fish par default
chsh -s $(which bash) bash par default


sudi iwconfig wlp3s0 power off

# A executer apres chaque freeze pour diagnostiquer l erreur
journalctl -b -1 -p err

a rechercher : x86/cpu: SGX disabled or unsupported by BIOS.

